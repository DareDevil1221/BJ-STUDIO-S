# Admin Security
# Secondary key required to access the admin portal even for authenticated admins
ADMIN_ACCESS_KEY=

# Firebase Configuration (Already handled by firebase-applet-config.json usually, 
# but project ID can be overridden)
FIREBASE_PROJECT_ID=
